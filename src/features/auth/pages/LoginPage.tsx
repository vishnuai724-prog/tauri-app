import { LoginForm } from "../components/LoginForm";

/**
 * Login page — router target for /login.
 * Wraps the LoginForm component as a page-level entry point.
 */
export default function LoginPage() {
  return <LoginForm />;
}
